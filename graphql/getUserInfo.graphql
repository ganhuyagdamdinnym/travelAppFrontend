query GetUserInfo($input: userTokenInput!) {
  getUserInfo(input: $input) {
    email
    name
    emergencyPhone
    currency
    favorates
    gender
    profileImageUrl
    point
    phoneNumber
    payment
  }
}
